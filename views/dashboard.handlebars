    <div class="">
        <a href="/dashboard/add/"><button id="add-post-form" class="btn-small">create post</button></a>
        
        <h2 class="">dashboard: your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="btn-up">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="">you do not currently have any posts</h6>

{{/if}}